console.log('oui');
$(document).ready(function(){
	$checkStatus = null;
	$nowStatus = null;
	$gameBoard = $("#game-board");
	$partyId = $("#party-id").html();
	setInterval( function() {

		$.ajax({
			url: $partyId + '/get_status?id=' + $partyId,
			type: "GET",
			dataType: 'json',
			success: function(json, status) {
				data = eval('('+json+')');
				console.log(json);
				console.log(data);
				changeStatus(data);
			}
		});
	}, 1500);
});

function checkStatus(status){
	if (status != $nowStatus) {
			$nowStatus = status;
			changeStatus($nowStatus);
		}
}
