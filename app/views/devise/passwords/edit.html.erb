<div class="d-flex justify-content-center flex-column align-items-center">
  <h2 class="short-hr-center">CHANGER VOTRE MOT DE PASSE</h2>

  <div id="contactForm" class="mb-5" style="min-width: 30rem">
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
    <%= f.hidden_field :reset_password_token %>

    <div class="form-group">
      <% if @minimum_password_length %>
      <% end %>
      <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: "form-control", placeholder: "Nouveau mot de passe (6 caractères min)"%>
      <div class="help-block with-errors"></div>
    </div>

    <div class="form-group">
      <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control", placeholder: "Confirmation du nouveau mot de passe" %>
      <div class="help-block with-errors"></div>
    </div>

    <%= f.button "Changer mon mot de passe", class: "button-devise-first" %>
    <% end %>

  </div>
</div>
