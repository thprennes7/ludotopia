<h1>Requêtes utilisateurs :</h1>

<% @supports.each do |support| %>
<%= render partial: 'messages', locals: { support: support } %>
<% end %>

<%= will_paginate @supports, renderer: BootstrapPagination::Rails %>
